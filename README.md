# 2023-09-06-git-basic
- `git init`: 
    - Turns the current folder (working dir) into a git repository
    - Tyoe this per folder structure
- `git status`:
    - Tells you what's going on with your git in your current repo
